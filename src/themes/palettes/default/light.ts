import type { PaletteOptions } from '@mui/material'

import { whiteColor } from './shared'

export const lightPaletteTokens: PaletteOptions = {
  background: {
    default: '#f4f5fa',
    paper: whiteColor,
  },
}
